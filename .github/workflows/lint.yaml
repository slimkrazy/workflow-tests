name: Lint

on: [workflow_call]

jobs:

  lint:
    name: Lint the code
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Run the  Linter
        run: |
          echo "${{github.ref}}"
          echo "Run the linter"
